# 功能性设计

做像素值比对功能，注意去耦合设计以保证可扩展性。
为模型迭代添加可选项，使得在触发迭代阈值或者手动停止时，也会直接保存模型结果。
画图应该给出数据来源

# 去耦合设计

应该为每份任务生成一份专属的坐标轴，这样每次画图可以直接调用
应该为每份任务生成一份清单，保存在result_{时间}文件夹中，它应该包含
- 对数据做了哪些处理
- 使用了什么模型，模型表征数据
- 光谱坐标轴

命令行设计

控制台输出信息尽可能简单

日志应该尽可能详细